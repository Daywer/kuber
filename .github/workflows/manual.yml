name: ARC Runner Minimal Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted
    steps:
      - run: |
          echo "Runner is working"
          echo "Hostname: $(hostname)"
          echo "User: $(whoami)"
